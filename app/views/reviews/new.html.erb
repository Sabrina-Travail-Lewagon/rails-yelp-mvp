<div class="container">
  <h1>Publier un avis</h1>
  <%= simple_form_for [@restaurant, @review] do |f| %>
    <%= f.input :content %>
    <%= f.input :rating %>
    <%= f.submit "Valider l'avis", class: "btn btn-primary" %>
  <% end %>
</div>
